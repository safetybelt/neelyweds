import React from 'react';
import styles from './photographs.scss';

const Photographs = () => (
    <section className={styles.photographs}>
        this is the photographs section
    </section>
);

export default Photographs;
